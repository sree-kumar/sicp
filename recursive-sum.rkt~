#lang racket
; recursive version of summing 2 numbers
; from lecture by Prof. Gerald Jay Sussman
; time = O(x)
; space = O(x)

(define (sum x y)
  (if (= x 0)
	  y
	  (add1 (+   x) y)))

(sum 3 4)

; execution sequence
; (sum 3 4)
; (sum 2 5)
; (sum 1 6)
; (sum 0 7)
